# Прод-зависимости (запинены под проверенные версии).
requests==2.32.5
pandas==3.0.1
numpy==2.4.2
python-dotenv==1.2.1
colorama==0.4.6
flask==3.1.3
yfinance==1.2.0

# WSGI-сервер для прод-развёртывания (Linux/Mac; на Windows используйте waitress).
gunicorn==23.0.0

# Тесты (dev). Установка: pip install -r requirements.txt
pytest==8.3.4
